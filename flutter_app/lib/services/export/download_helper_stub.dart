void downloadFileImpl({
  required String fileName,
  required String content,
  required String mimeType,
}) {
  // Non-web / test stub. On Flutter Web, download_helper_web.dart handles this via Blob.
}
